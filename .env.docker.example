# Postgres configuration
POSTGRES_USER=legalflow_admin
POSTGRES_PASSWORD=change_me_in_local_only
POSTGRES_DB=legalflow_prod

# MinIO configuration
MINIO_ROOT_USER=admin_minio
MINIO_ROOT_PASSWORD=change_me_in_local_only
