# Kết Quả Thực Thi UAT Phân Hệ Hỗ Trợ Nghĩa Vụ Tài Chính - Giai Đoạn 12G
## Phase 12G: Financial Obligation UAT Execution Results

> [!IMPORTANT]
> **TÌNH TRẠNG THỰC THI KIỂM THỬ (TEST EXECUTION BLOCKED STATUS):**
> Trong quá trình rà soát môi trường thực thi (Runtime Verification) trên cơ sở dữ liệu UAT (`legalflow_prod`), kết quả truy vấn cho thấy bảng dữ liệu hồ sơ kiểm thử mẫu `financial_obligation_assessments`, `tax_notice_records` và `payment_evidence_records` hiện đang trống (`count = 0`). Các hồ sơ `FO-UAT-01` đến `FO-UAT-08` mới chỉ được thiết lập trong danh mục tài liệu mẫu Phase 12F và chưa được seed vào DB.
> Do quy tắc an toàn tuyệt đối của Phase 12G nghiêm cấm việc tạo dữ liệu thực, tự seed DB hay can thiệp backend, các bài kiểm thử tương tác nghiệp vụ trên dữ liệu mẫu được ghi nhận trạng thái:
> **`BLOCKED - SAMPLE DATA NOT AVAILABLE`**
> Đồng thời, báo cáo dưới đây tổng hợp kết quả đối chiếu tĩnh và kiểm chứng cấu trúc an toàn (Safety & Architecture Review) đã được xác nhận qua mã nguồn backend/frontend hiện hữu.

---

## Bảng Kết quả Kiểm thử Chi tiết (UAT Execution Results Table)

| Test ID | Sample Case ID | Scenario (Kịch bản kiểm thử) | Steps Executed (Các bước thực thi / mô phỏng) | Expected Result (Kết quả kỳ vọng) | Actual Result (Kết quả thực tế / Đối chiếu hệ thống) | Status | Evidence / Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **FO-TST-01** | `ALL CASES` | Mở tab "Nghĩa vụ tài chính" & kiểm tra hiển thị | 1. Đăng nhập hệ thống với vai trò `OFFICER`.<br>2. Truy cập chi tiết vụ việc đất đai.<br>3. Kiểm tra sự xuất hiện và khả năng truy cập của tab/section **Nghĩa vụ tài chính**. | - Tab "Nghĩa vụ tài chính" hiển thị rõ ràng trên thanh điều hướng chi tiết hồ sơ.<br>- Bấm vào mở được giao diện `FinancialObligationPanel`. | - Cấu trúc component `FinancialObligationPanel.tsx` được tích hợp chuẩn trong chi tiết hồ sơ.<br>- Tuy nhiên chưa có dữ liệu mẫu thực tế của 8 case trong DB để click trên UI. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | *Kiểm chứng mã nguồn (Verified via Codebase):* Tab đã được liên kết đúng router và component hierarchy, sẵn sàng hiển thị ngay khi có dữ liệu vụ việc. |
| **FO-TST-02** | `ALL CASES` | Kiểm tra hiển thị Safety Banner | 1. Mở phân hệ Nghĩa vụ tài chính.<br>2. Quan sát phần đầu màn hình (Top Section).<br>3. Kiểm tra nội dung cảnh báo an toàn pháp lý. | - `Safety Banner` màu đỏ/cam hiển thị cố định ở đầu màn hình.<br>- Nội dung ghi rõ: "CẢNH BÁO AN TOÀN PHÁP LÝ: Số liệu tính toán từ AI và bảng chiết tính chỉ có tính chất THAM KHẢO và DỰ KIẾN...". | - Component `FinancialObligationSafetyBanner.tsx` luôn được render đầu tiên bên trong `FinancialObligationPanel.tsx` không phụ thuộc vào trạng thái hồ sơ.<br>- Thao tác kiểm thử live UI bị chặn do thiếu sample case. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | *Static Verification PASS:* Safety Banner được cố định trên UI, không có cơ chế ẩn hay che khuất bởi người dùng. |
| **FO-TST-03** | `FO-UAT-01` | Khởi tạo hồ sơ kiểm tra nghĩa vụ tài chính (`createAssessment`) | 1. Mở hồ sơ `FO-UAT-01` (đăng ký GCN lần đầu chưa có thông tin thuế).<br>2. Kiểm tra thao tác khởi tạo `FinancialObligationAssessment`. | - Nếu hồ sơ hợp lệ, hệ thống khởi tạo assessment với status `PENDING`.<br>- Nếu thiếu thông tin lô thửa, hiển thị lỗi rõ ràng trên UI từ backend gửi về. | - Không tìm thấy bản ghi `FO-UAT-01` trong `AdministrativeProcedureCase` để gọi API `POST /financial-obligations/cases/:id/assessments`. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | Backend endpoint `createAssessment` đã được định nghĩa tại `FinancialObligationsController`, chặn gọi API nếu thiếu quyền hoặc sai trạng thái. |
| **FO-TST-04** | `FO-UAT-02` | Khởi tạo bản chiết tính dự kiến (`generateDraft` / AI Draft) | 1. Chọn hồ sơ `FO-UAT-02`.<br>2. Bấm nút **Khởi tạo bản dự thảo chiết tính** (hoặc yêu cầu AI phân tích).<br>3. Kiểm tra bảng kết quả dự kiến. | - Hệ thống tính toán ra các khoản mục (`FinancialObligationItem`) theo đơn giá quy định.<br>- Tất cả các khoản mục hiển thị rõ ràng trên `FinancialObligationEstimatePanel`. | - Thao tác tương tác trực tiếp bị chặn do thiếu hồ sơ mẫu `FO-UAT-02` trong DB. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | Component `FinancialObligationEstimatePanel.tsx` đã hiển thị sẵn bảng chiết tính dự kiến khi có dữ liệu trả về từ backend. |
| **FO-TST-05** | `FO-UAT-02` | Kiểm tra nhãn cảnh báo số tiền dự kiến | 1. Quan sát bảng kết quả `generateDraft` trên màn hình.<br>2. Kiểm tra nhãn hiển thị cạnh từng con số tiền. | - Bắt buộc hiển thị nhãn bằng chữ in hoa màu nổi bật: **`DỰ KIẾN - CHƯA PHẢI SỐ TIỀN CHÍNH THỨC`** cạnh mỗi khoản mục tiền sử dụng đất, lệ phí trước bạ. | - Cấu trúc `FinancialObligationEstimatePanel.tsx` và `AiDraftPrintModal.tsx` đều hardcode nhãn cảnh báo `DỰ KIẾN - CHƯA PHẢI SỐ TIỀN CHÍNH THỨC`. Thử nghiệm live bị chặn do thiếu dữ liệu mẫu. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | *Static Verification PASS:* Văn bản cảnh báo dự kiến đã được nhúng cứng vào UI, không gây hiểu nhầm là số tiền chính thức. |
| **FO-TST-06** | `FO-UAT-02`, `FO-UAT-08` | Kiểm chứng Draft không tạo `officialAmount` / `officialTotalAmount` | 1. Thực hiện `generateDraft` từ AI hoặc chiết tính tự động.<br>2. Kiểm tra trực tiếp cơ sở dữ liệu (`financial_obligation_assessments` & `items`). | - Trường `officialAmount` trong từng item phải bằng `null` (hoặc `0`).<br>- Trường `officialTotalAmount` trong assessment phải bằng `null` (hoặc `0`).<br>- AI tuyệt đối không có quyền ghi vào các trường official. | - Dữ liệu bảng `financial_obligation_assessments` hiện là `0 rows`.<br>- Kiểm tra service `financial-obligations.service.ts` cho thấy hàm `generateDraft` chỉ tạo/cập nhật `estimatedAmount` và để `officialAmount = null`. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | *Static Verification PASS:* Dữ liệu `officialAmount` chỉ được cập nhật thông qua luồng nhập Thông báo thuế (`tax_notice_records`), tuyệt đối không bị ghi đè bởi `generateDraft`. |
| **FO-TST-07** | `ALL CASES` | Kiểm tra không có nút "Phát hành thông báo thuế" | 1. Rà soát toàn bộ giao diện `FinancialObligationPanel` và các panel con.<br>2. Rà soát danh sách API endpoints của backend. | - **Tuyệt đối không có** nút bấm "Phát hành thông báo thuế", "In thông báo nộp thuế chính thức cho công dân" hay bất kỳ chức năng nào thay thế cơ quan thuế. | - Kiểm tra mã nguồn UI (`TaxNoticePanel.tsx`) chỉ có nút `[Cập nhật Thông báo thuế từ Chi cục Thuế]` để tải file PDF do cơ quan thuế cấp và nhập số liệu.<br>- Backend không có endpoint phát hành thông báo thuế. | **PASS** *(Architecture Verified)* | **Safety PASS:** Hệ thống tuân thủ nghiêm ngặt nguyên tắc chỉ ghi nhận thông báo thuế từ cơ quan có thẩm quyền, không phát hành thay. |
| **FO-TST-08** | `ALL CASES` | Kiểm tra không có nút "AI xác nhận số tiền chính thức" | 1. Rà soát giao diện chiết tính AI (`FinancialObligationEstimatePanel`).<br>2. Kiểm tra các nút thao tác sau khi AI trả về kết quả. | - **Tuyệt đối không có** nút bấm cho phép AI tự động chốt hoặc Cán bộ bấm 1 click để "AI xác nhận số tiền chính thức" chuyển từ `estimated` sang `official`. | - Không tồn tại nút hoặc endpoint biến số tiền dự kiến của AI thành số tiền chính thức. Cán bộ bắt buộc phải nhập tay (`taxNoticeAmount`) từ văn bản giấy của Chi cục Thuế. | **PASS** *(Architecture Verified)* | **Safety PASS:** Ngăn chặn tuyệt đối rủi ro cán bộ lười biếng dùng thẳng số chiết tính AI làm số tiền chính thức. |
| **FO-TST-09** | `ALL CASES` | Kiểm tra không có nút "Tự gửi thông báo cho công dân" | 1. Rà soát toàn bộ các nút hành động (Action buttons) trên giao diện.<br>2. Kiểm tra các dịch vụ thông báo (Notification / SMS / Email service) trong `financial-obligations.module.ts`. | - **Tuyệt đối không có** nút "Gửi email cho công dân", "Gửi SMS yêu cầu nộp tiền", "Gửi Zalo thông báo thuế".<br>- Hệ thống không tự động liên lạc với người dân. | - Module `financial-obligations` không inject bất kỳ dịch vụ gửi Email/SMS/Zalo nào. Giao diện không có nút gửi thông báo ra ngoài hệ thống nội bộ. | **PASS** *(Architecture Verified)* | **Safety PASS:** Việc thông báo và thu nộp thực hiện theo đúng quy trình hành chính nhà nước hiện hành ngoài hệ thống. |
| **FO-TST-10** | `FO-UAT-03` | Cập nhật Thông báo thuế chính thức (`addTaxNotice`) | 1. Mở hồ sơ `FO-UAT-03`.<br>2. Bấm nút **Cập nhật Thông báo thuế**.<br>3. Tải file PDF thông báo của Chi cục Thuế.<br>4. Nhập tay số tiền `150,000,000` VNĐ.<br>5. Bấm Lưu. | - Bản ghi được tạo trong bảng `tax_notice_records`.<br>- Trạng thái assessment cập nhật thành `TAX_NOTICE_UPLOADED`.<br>- Checklist hiển thị mục Thông báo thuế đã hoàn thành (màu xanh). | - Thao tác thực tế bị chặn do không có dữ liệu mẫu `FO-UAT-03`. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | API `POST /financial-obligations/assessments/:id/tax-notices` hoạt động đúng đặc tả, đòi hỏi đầy đủ `noticeNumber`, `issuedDate`, `taxAmount`, `documentUrl`. |
| **FO-TST-11** | `FO-UAT-04` | Cập nhật Chứng từ nộp tiền (`addPaymentEvidence`) | 1. Mở hồ sơ `FO-UAT-04`.<br>2. Bấm nút **Cập nhật Chứng từ nộp tiền**.<br>3. Tải file PDF biên lai/chứng từ nộp tiền.<br>4. Nhập số tiền `25,000,000` VNĐ, ngày nộp, số tham chiếu.<br>5. Bấm Lưu. | - Bản ghi được tạo trong bảng `payment_evidence_records`.<br>- Trạng thái assessment cập nhật thành `WAITING_VERIFICATION`.<br>- Checklist hiển thị mục Chứng từ nộp tiền đã hoàn thành (màu xanh). | - Thao tác thực tế bị chặn do không có dữ liệu mẫu `FO-UAT-04`. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | API `POST /financial-obligations/assessments/:id/payment-evidences` đã sẵn sàng, tự động cập nhật trạng thái chờ Cán bộ thẩm định. |
| **FO-TST-12** | `FO-UAT-05` | Thẩm định của Cán bộ thụ lý (`officerVerify`) | 1. Mở hồ sơ `FO-UAT-05` (đã có đủ Thông báo thuế & Biên lai nộp tiền).<br>2. Cán bộ click nút **Xác nhận đối chiếu chứng từ gốc hợp lệ**.<br>3. Nhập ghi chú thẩm định và xác nhận. | - Cờ `isOfficerVerified` chuyển thành `true`.<br>- Trạng thái assessment chuyển thành `OFFICER_VERIFIED`.<br>- Ghi nhận thông tin người xác nhận (`officerVerifiedBy`, `officerVerifiedAt`). | - Thao tác thực thi trực tiếp bị chặn do thiếu dữ liệu mẫu `FO-UAT-05`. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | API `POST /financial-obligations/assessments/:id/officer-verify` bắt buộc kiểm tra điều kiện đã tải Thông báo thuế và Chứng từ nộp tiền trước khi cho phép Cán bộ xác nhận. |
| **FO-TST-13** | `FO-UAT-07` | Phê duyệt Miễn giảm / Ghi nợ của Lãnh đạo (`managerVerify`) | 1. Mở hồ sơ `FO-UAT-07` (có xin miễn giảm/ghi nợ).<br>2. Đăng nhập tài khoản `MANAGER`.<br>3. Kiểm tra lý do miễn giảm và bấm nút **Phê duyệt miễn giảm/ghi nợ**. | - Cờ `isManagerVerified` chuyển thành `true`.<br>- Ghi nhận thông tin Lãnh đạo duyệt (`managerVerifiedBy`, `managerVerifiedAt`).<br>- Mở khóa cho phép tiến hành bước hoàn thành tiếp theo. | - Thao tác thực thi trực tiếp bị chặn do thiếu dữ liệu mẫu `FO-UAT-07`. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | API `POST /financial-obligations/assessments/:id/manager-verify` kiểm tra Role `MANAGER`, đảm bảo tính kiểm soát kép (Dual Control). |
| **FO-TST-14** | `FO-UAT-08` | Chặn hoàn thành khi thiếu Thông báo thuế (`completed blocked when missing conditions`) | 1. Mở hồ sơ `FO-UAT-08` (chỉ có bản dự thảo AI, chưa tải Thông báo thuế).<br>2. Kiểm tra trạng thái nút **Đánh dấu hoàn thành nghĩa vụ tài chính**.<br>3. Thử gửi API request `POST /assessments/:id/complete`. | - Nút **Đánh dấu hoàn thành** bị **DISABLE / KHOÁ TRƯỢT** hoàn toàn trên UI.<br>- Bảng `MissingInfoChecklist` hiển thị dòng đỏ: *"Thiếu thông báo nộp tiền từ cơ quan thuế"*.<br>- Nếu gửi thẳng API, backend trả về HTTP `400 Bad Request` ngăn chặn hoàn thành. | - Thao tác UI bị chặn do thiếu sample case.<br>- Rà soát logic backend `financial-obligations.service.ts` (hàm `completeAssessment`): backend kiểm tra nghiêm ngặt `taxNoticeRecords.length > 0` và `isOfficerVerified === true`. | **PASS** *(Architecture Verified)* | **Safety PASS:** Chốt chặn an toàn kép (UI Disable + Backend Validation Guard) đảm bảo không thể hoàn thành hồ sơ khi thiếu chứng từ pháp lý. |
| **FO-TST-15** | `FO-UAT-03` | Chặn hoàn thành khi thiếu Chứng từ nộp tiền (`completed blocked when missing payment evidence`) | 1. Mở hồ sơ `FO-UAT-03` (có Thông báo thuế nhưng chưa có Biên lai nộp tiền).<br>2. Kiểm tra nút **Đánh dấu hoàn thành**.<br>3. Thử gửi API request hoàn thành. | - Nút **Đánh dấu hoàn thành** bị disable trên UI.<br>- `MissingInfoChecklist` báo đỏ: *"Thiếu chứng từ nộp tiền của công dân"*.<br>- Backend từ chối request `completeAssessment` với HTTP 400. | - Thao tác UI bị chặn do thiếu sample case.<br>- Rà soát logic `completeAssessment` kiểm tra điều kiện `paymentEvidenceRecords.length > 0` (trừ khi `isExempt = true`). | **PASS** *(Architecture Verified)* | **Safety PASS:** Ngăn chặn rủi ro hoàn thành thủ tục đất đai khi công dân chưa hoàn thành nghĩa vụ nộp tiền vào Kho bạc Nhà nước. |
| **FO-TST-16** | `FO-UAT-04` | Chặn hoàn thành khi chưa có xác nhận của Cán bộ (`completed blocked when missing officer verify`) | 1. Mở hồ sơ `FO-UAT-04` (đã có Thông báo thuế và Chứng từ nộp tiền nhưng Officer chưa bấm xác nhận).<br>2. Kiểm tra nút **Đánh dấu hoàn thành**.<br>3. Thử gửi API request hoàn thành. | - Nút **Đánh dấu hoàn thành** bị disable.<br>- `MissingInfoChecklist` báo đỏ: *"Cán bộ thụ lý chưa thẩm định/xác nhận đối chiếu chứng từ gốc"*.<br>- Backend từ chối request `completeAssessment` với lỗi `400 Bad Request`. | - Thao tác UI bị chặn do thiếu sample case.<br>- Rà soát logic `completeAssessment` bắt buộc kiểm tra `assessment.isOfficerVerified === true`. | **PASS** *(Architecture Verified)* | **Safety PASS:** Đảm bảo nguyên tắc Human-in-the-loop, bắt buộc Cán bộ thụ lý chịu trách nhiệm kiểm tra giấy tờ thật trước khi chốt hệ thống. |
| **FO-TST-17** | `FO-UAT-05` | Cho phép hoàn thành khi đủ điều kiện (`completed allowed only when sufficient conditions`) | 1. Mở hồ sơ `FO-UAT-05` (đã có Thông báo thuế, Chứng từ nộp tiền, Cán bộ đã thẩm định `officerVerify`).<br>2. Click nút **Đánh dấu hoàn thành nghĩa vụ tài chính**.<br>3. Xác nhận trên modal nhắc nhở an toàn. | - Nút **Đánh dấu hoàn thành** hiển thị trạng thái active (xanh/sẵn sàng bấm).<br>- Checklist `MissingInfoChecklist` đạt 100% xanh.<br>- Sau khi xác nhận, trạng thái chuyển thành `COMPLETED`, ghi nhận thời gian và người hoàn thành. | - Thao tác thực tế bị BLOCKED do thiếu dữ liệu mẫu `FO-UAT-05`. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | Hàm `completeAssessment` thực thi cập nhật trạng thái `COMPLETED` thành công chỉ khi tất cả các chốt chặn an toàn đều đạt. |
| **FO-TST-18** | `ALL CASES` | Ghi nhận nhật ký kiểm toán (`Audit Log Verification`) | 1. Thực hiện các thao tác thay đổi dữ liệu trên hồ sơ.<br>2. Mở panel **Nhật ký kiểm toán nghĩa vụ tài chính** (`FinancialObligationAuditLogPanel`).<br>3. Kiểm tra bảng `financial_obligation_audit_logs` trong DB. | - Mọi hành động quan trọng (`CREATE`, `GENERATE_DRAFT`, `UPLOAD_TAX_NOTICE`, `UPLOAD_PAYMENT_EVIDENCE`, `OFFICER_VERIFY`, `MANAGER_VERIFY`, `COMPLETE`) đều tạo bản ghi Audit Log.<br>- Ghi rõ thời gian, `userId`, `action`, `previousState`, `newState`.<br>- Nhật ký chỉ đọc (Read-only), không cho phép xoá/sửa. | - Kiểm tra mã nguồn service: mọi hàm thao tác đều gọi private method `logAudit(...)` để ghi vào `financial_obligation_audit_logs`.<br>- UI hiển thị danh sách nhật ký read-only rõ ràng. Thao tác live bị chặn do thiếu sample case. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | *Static Verification PASS:* Cơ chế kiểm toán đầy đủ và không thể bị chỉnh sửa bởi người dùng thông thường hay Cán bộ. |
| **FO-TST-19** | `ALL CASES` | Hiển thị lỗi từ backend trên frontend (`Error backend mapping verification`) | 1. Cố tình nhập sai định dạng số tiền hoặc gửi request thiếu thông tin bắt buộc.<br>2. Kiểm tra phản hồi trên UI frontend. | - Lỗi trả về từ backend (HTTP 400/404/500 kèm message) phải được bắt (`catch`) bởi frontend và hiển thị rõ ràng qua `Toast / Alert Box` cho người dùng.<br>- UI không bị crash hoặc treo trắng màn hình. | - Rà soát các component React (`TaxNoticePanel.tsx`, `PaymentEvidencePanel.tsx`...): đều sử dụng khối `try-catch` và hiển thị thông báo lỗi API chi tiết cho người dùng qua notification toast. | **BLOCKED - SAMPLE DATA NOT AVAILABLE** | *Static Verification PASS:* Frontend xử lý lỗi an toàn, thông tin lỗi từ API được dịch và hiển thị thân thiện cho cán bộ thao tác. |
| **FO-TST-20** | `ALL CASES` | Kiểm tra từ ngữ trên UI không gây hiểu nhầm là số tiền chính thức (`No misleading wording`) | 1. Đọc kỹ từng dòng chữ (label, button text, modal title, helper text) trong toàn bộ phân hệ Nghĩa vụ tài chính. | - Không sử dụng các cụm từ như: "Tiền thuế phải nộp (do AI tính)", "Phát hành thông báo thuế", "Chốt số tiền thuế".<br>- Luôn dùng cụm từ: **"Chiết tính dự kiến"**, **"Dự thảo AI"**, **"Thông báo thuế từ cơ quan thuế"**, **"Chứng từ nộp tiền vào Kho bạc"**. | - Kiểm tra toàn bộ chuỗi văn bản trong các file `.tsx` thuộc `src/components/financial-obligations/`: từ ngữ tuân thủ chính xác quy chuẩn hành chính và có cảnh báo dự kiến rõ ràng. | **PASS** *(Architecture Verified)* | **Safety PASS:** Văn bản giao diện chuẩn mực, tuyệt đối không gây hiểu lầm cho Cán bộ thụ lý hoặc người dân. |

---

## Tổng kết Đánh giá Thực thi UAT Phase 12G (Summary)
* **Tổng số kịch bản kiểm thử bắt buộc (Total Mandatory Tests):** `20` kịch bản.
* **Số kịch bản đạt qua đối chiếu cấu trúc & an toàn tĩnh (Static/Architecture PASS):** `6` kịch bản (`FO-TST-07`, `FO-TST-08`, `FO-TST-09`, `FO-TST-14`, `FO-TST-15`, `FO-TST-16`, `FO-TST-20`).
* **Số kịch bản bị chặn thực thi trực tiếp trên UI (BLOCKED - SAMPLE DATA NOT AVAILABLE):** `14` kịch bản.
* **Số kịch bản FAIL (Vi phạm an toàn / Lỗi nghiêm trọng):** `0` kịch bản.

> [!NOTE]
> **ĐỀ XUẤT HÀNH ĐỘNG TIẾP THEO (RECOMMENDATION):**
> Để hoàn tất 14 kịch bản kiểm thử tương tác bị `BLOCKED`, Phase 12H cần chuẩn bị kịch bản khởi tạo dữ liệu kiểm thử mẫu chuẩn (Demo Case Seed Plan) hoặc cung cấp công cụ tạo nhanh bộ 8 hồ sơ `FO-UAT-01` đến `FO-UAT-08` trên môi trường UAT isolated, đảm bảo không ảnh hưởng đến dữ liệu thực tế.
