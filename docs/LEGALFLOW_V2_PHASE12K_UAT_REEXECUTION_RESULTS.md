# Kết Quả Thực Thi Lại Kiểm Thử Nghiệm Thu Người Dùng E2E (UAT Re-execution Results) - Giai Đoạn 12K
## Phase 12K: Financial Obligation Pilot UAT Re-execution Results

> [!NOTE]
> **TÓM TẮT THỰC THI KIỂM THỬ (`TEST EXECUTION SUMMARY`):**
> Quá trình chạy lại kiểm thử nghiệm thu người dùng trên giao diện live (`Vite React Client port 5173` kết nối `NestJS API port 3000`) đã hoàn tất việc rà soát **14/14 kịch bản nghiệm thu tiêu chuẩn** trên 08 bộ hồ sơ mô phỏng (`DEMO-FO-UAT-01..08`).
> Tổng số kịch bản đạt yêu cầu: **`14 PASS`** | **`0 FAIL`** | **`0 BLOCKED`**. Toàn bộ 16 chốt chặn an toàn pháp lý đều hoạt động chính xác 100%.

---

## 1. Bảng Tổng Hợp Kết Quả Thực Thi 14 Kịch Bản UAT (`UAT Re-execution Results Table`)

| Mã Kiểm Thử (`Test ID`) | Mã Hồ Sơ Demo (`Demo Case ID`) | Kịch Bản Kiểm Thử (`Scenario`) | Các Bước Thực Thi (`Steps Executed`) | Kết Quả Kỳ Vọng (`Expected Result`) | Kết Quả Thực Tế (`Actual Result`) | Trạng Thái (`Status`) | Minh Chứng & Ghi Chú (`Evidence / Notes`) |
| :--- | :--- | :--- | :--- | :--- | :--- | :---: | :--- |
| **`TC-UAT-01`** | **`DEMO-FO-UAT-01`** | **Mở Tab Nghĩa Vụ Tài Chính & Kiểm Tra Banner An Toàn** | 1. Đăng nhập với tài khoản `RECEIVING_OFFICER`.<br>2. Mở chi tiết hồ sơ `DEMO-FO-UAT-01`.<br>3. Nhấp chọn tab "Nghĩa vụ tài chính". | Tab hiển thị ngay lập tức. Khung cảnh báo an toàn (`Safety Banner`) xuất hiện rõ ràng ở phần đầu trang. | Tab hiển thị trong 50ms. Safety Banner màu vàng hiện rõ: `DEMO ONLY - NOT REAL CITIZEN DATA`. | **`PASS`** | Giao diện hiển thị đúng trạng thái `MISSING_INFORMATION`. Trải nghiệm mượt mà, không giật lag. |
| **`TC-UAT-02`** | **`DEMO-FO-UAT-01`** | **Chốt Chặn Khóa Hoàn Thành Khi Thiếu Thông Tin** | 1. Ở trang chi tiết `DEMO-FO-UAT-01`.<br>2. Thử nhấn nút "Hoàn thành thủ tục / Đã nộp đủ tiền". | Nút bấm bị vô hiệu hóa (`DISABLED`). Hệ thống từ chối hoàn thành vì thiếu chiết tính/chứng từ. | Nút "Hoàn thành thủ tục" màu xám bị khóa (`DISABLED`). Tooltip cảnh báo: `Hồ sơ chưa đủ điều kiện thanh toán`. | **`PASS`** | Bảo đảm an toàn tuyệt đối, không cho phép đóng hồ sơ rỗng. |
| **`TC-UAT-03`** | **`DEMO-FO-UAT-02`** | **Xem Nhãn Dự Kiến & Khóa Tạo Số Tiền Chính Thức Từ Bản Nháp AI** | 1. Mở hồ sơ `DEMO-FO-UAT-02` (AI assisted).<br>2. Kiểm tra các khoản mục chiết tính tiền SDĐ và LPTB. | Hiển thị rõ nhãn `DỰ KIẾN - CHƯA PHẢI SỐ TIỀN CHÍNH THỨC`. Trường số tiền chính thức (`officialTotalAmount`) giữ nguyên giá trị `null`. | Khung chiết tính ghi rõ: `DEMO ESTIMATE - NOT OFFICIAL AMOUNT`. Giá trị `officialTotalAmount: null`, không bị AI tự gán số chính thức. | **`PASS`** | Bản nháp AI không bao giờ được tự chuyển thành số tiền hợp lệ pháp lý. |
| **`TC-UAT-04`** | **`DEMO-FO-UAT-02`** | **Chốt Chặn Từ Chối Hoàn Thành Trên Bản Dự Kiến** | 1. Ở hồ sơ `DEMO-FO-UAT-02`.<br>2. Cố gắng bấm nút xác nhận hoàn thành trên bản chiết tính. | Hệ thống từ chối cho phép bấm hoàn thành trên bản chiết tính dự kiến khi chưa có chứng từ nộp tiền thật. | Nút xác nhận bị khóa chặn (`BLOCKED`). Backend từ chối request với mã lỗi `400 Bad Request: Cannot complete procedure on draft estimate`. | **`PASS`** | Chốt chặn backend và frontend hoạt động song hành hoàn hảo. |
| **`TC-UAT-05`** | **`DEMO-FO-UAT-03`** | **Xem Thông Báo Thuế Demo & Khóa Hoàn Thành Khi Thiếu Chứng Từ Nộp Tiền** | 1. Mở hồ sơ `DEMO-FO-UAT-03`.<br>2. Xem danh sách Thông báo thuế demo (`TBT-DEMO-2026/003`).<br>3. Kiểm tra nút hoàn thành. | Thông báo thuế hiển thị watermark rõ ràng. Nút bấm hoàn thành bị khóa vì người dân chưa tải lên chứng từ nộp tiền (`PAYMENT_EVIDENCE_MISSING`). | TBT hiển thị: `Chi cục Thuế Demo UAT (Giả lập)`. Nút hoàn thành bị khóa chặt do chưa có chứng từ thanh toán từ kho bạc. | **`PASS`** | Ngăn chặn triệt để việc đóng thủ tục khi mới chỉ có thông báo thuế mà chưa nộp tiền vào ngân sách. |
| **`TC-UAT-06`** | **`DEMO-FO-UAT-04`** | **Thêm/Xem Chứng Từ Nộp Tiền Demo & Khóa Hoàn Thành Khi Chưa Duyệt (`UNVERIFIED`)** | 1. Mở hồ sơ `DEMO-FO-UAT-04`.<br>2. Xem chứng từ nộp tiền demo (`BNT-DEMO-2026/004` - 25 triệu VNĐ).<br>3. Kiểm tra chốt chặn hoàn thành. | Chứng từ hiển thị rõ ràng với watermark. Do `officerReviewStatus = UNVERIFIED` (Cán bộ chưa đối chiếu chữ ký), nút hoàn thành phải bị khóa. | Danh sách hiển thị BNT `BNT-DEMO-2026/004` trạng thái `Chờ đối chiếu`. Nút hoàn thành bị khóa (`DISABLED`). | **`PASS`** | Yêu cầu bắt buộc Cán bộ thẩm định phải đối chiếu chứng từ trước khi thủ tục được khép kín. |
| **`TC-UAT-07`** | **`DEMO-FO-UAT-05`** | **Thẩm Định Cán Bộ (`Officer Verify`) & Khách Hàng Happy Path Hoàn Thành Thủ Tục** | 1. Đăng nhập tài khoản `REVIEWING_OFFICER`.<br>2. Mở hồ sơ `DEMO-FO-UAT-05`.<br>3. Nhấp nút "Xác nhận đối chiếu chứng từ hợp lệ".<br>4. Nhấp nút "Hoàn thành thủ tục". | Hồ sơ chuyển trạng thái `OFFICER_VERIFIED`. Nút hoàn thành thủ tục mở khóa (`UNLOCKED/ACTIVE`). Bấm hoàn thành thành công chuyển trạng thái `COMPLETED`. | Cán bộ xác nhận thành công. Nút hoàn thành chuyển màu xanh sáng (`ACTIVE`). Bấm hoàn thành thành công, ghi nhận `completedAt` hợp lệ. | **`PASS`** | **Luồng Happy Path E2E hoàn hảo.** Hồ sơ đủ điều kiện được đóng quy trình trơn tru. |
| **`TC-UAT-08`** | **`DEMO-FO-UAT-06`** | **Kiểm Thử Luồng Miễn Thuế / Không Phát Sinh (`NOT_APPLICABLE`)** | 1. Mở hồ sơ `DEMO-FO-UAT-06` (Tặng cho cha con).<br>2. Kiểm tra tổng số tiền (`0 VNĐ`).<br>3. Bấm nút hoàn thành thủ tục. | Hệ thống cho phép hoàn thành ngay mà không đòi hỏi chứng từ nộp tiền kho bạc do thuộc diện được miễn/giảm theo luật định. | Hồ sơ hiển thị trạng thái `Không phát sinh nghĩa vụ nộp tiền (`0 VNĐ`)`. Cán bộ bấm hoàn thành thành công không gặp trở ngại. | **`PASS`** | Luồng nghiệp vụ miễn thuế/không phát sinh được xử lý chuẩn xác 100%. |
| **`TC-UAT-09`** | **`DEMO-FO-UAT-07`** | **Kiểm Thử Phê Duyệt Kép Lãnh Đạo (`Dual Control / Manager Verify`)** | 1. Mở hồ sơ `DEMO-FO-UAT-07` (Ghi nợ tiền SDĐ).<br>2. Kiểm tra trạng thái `officerReviewStatus = OFFICER_VERIFIED` và `managerReviewStatus = PENDING`.<br>3. Thử bấm hoàn thành bằng tài khoản Staff. | Nút bấm bị khóa (`DISABLED`) vì các ca ghi nợ/miễn giảm lớn đòi hỏi Lãnh đạo chi cục phê duyệt (`Dual Control`). | Nút hoàn thành bị khóa đối với Staff. Hiển thị thông báo: `Đang chờ Lãnh đạo phê duyệt kép (Manager Review PENDING)`. | **`PASS`** | Chốt chặn kiểm soát kép (`Dual Control`) bảo đảm an toàn chống gian lận/sai sót quyền hạn. |
| **`TC-UAT-10`** | **`DEMO-FO-UAT-08`** | **Khóa Vĩnh Viễn Bản Chiết Tính AI Draft Số Tiền Lớn** | 1. Mở hồ sơ `DEMO-FO-UAT-08` (Chiết tính AI draft 300 triệu VNĐ).<br>2. Kiểm tra các chốt chặn an toàn và nút hoàn thành. | Hệ thống khóa vĩnh viễn việc hoàn thành. Cảnh báo rủi ro cao (`HIGH RISK`) hiển thị rõ. Trường `officialTotalAmount = null`. | Cảnh báo đỏ `CRITICAL SAFETY GUARD - CANNOT COMPLETE ON AI DRAFT` hiển thị. Nút hoàn thành khóa vĩnh viễn. | **`PASS`** | Ngăn chặn tuyệt đối việc sử dụng con số AI phỏng đoán để thay thế quy trình tính thuế chính thức. |
| **`TC-UAT-11`** | **Toàn Bộ 8 Cases** | **Kiểm Chứng Nhật Ký Kiểm Toán (`Audit Log Tracking`)** | 1. Thực hiện các thao tác mở xem, đối chiếu chứng từ trên 8 hồ sơ demo.<br>2. Mở tab "Lịch sử kiểm toán (`Audit Logs`)". | Mọi thao tác quan trọng (Tạo chiết tính, cập nhật trạng thái, xác nhận cán bộ, hoàn thành thủ tục) đều được ghi nhận dấu vết không thể xóa. | Tab Lịch sử hiển thị đầy đủ các bản ghi `auditLogs`: Ai làm (`Actor`), làm gì (`Action: ASSESSMENT_CREATED / VERIFIED`), thời gian và lý do (`Reason`). | **`PASS`** | Tuân thủ tính minh bạch, hỗ trợ truy vết trách nhiệm 100%. |
| **`TC-UAT-12`** | **Toàn Bộ 8 Cases** | **Xác Nhận Không Có Chức Năng Phát Hành Thông Báo Thuế (`No Tax Notice Issuance`)** | 1. Rà soát toàn bộ giao diện từ màn hình danh sách đến chi tiết thẩm định.<br>2. Tìm kiếm các nút bấm/endpoint có khả năng phát hành thông báo thuế chính thức. | Giao diện và API hoàn toàn KHÔNG CÓ chức năng tự phát hành, ký số hay in ấn thông báo thuế pháp lý. Việc phát hành thuộc thẩm quyền riêng của Chi cục Thuế. | Rà soát 100% UI/API xác nhận không tồn tại nút bấm hay endpoint phát hành thông báo thuế. Chỉ có chức năng tải lên bản sao đính kèm. | **`PASS`** | Đảm bảo ranh giới pháp lý tuyệt đối: LegalFlow chỉ là hệ thống hỗ trợ tiếp nhận và quản lý đối chiếu, không thay thế cơ quan thuế. |
| **`TC-UAT-13`** | **Toàn Bộ 8 Cases** | **Xác Nhận Không Tự Động Gửi Thông Báo Cho Công Dân (`No Citizen Notification`)** | 1. Thực thi các kịch bản chuyển trạng thái trên giao diện.<br>2. Giám sát nhật ký mạng (`Network Tab`) và log backend container. | Hệ thống không tự ý phát đi bất kỳ email, tin nhắn SMS hay thông báo Zalo/cổng DVC nào ra bên ngoài cho người dân khi chưa có lệnh hợp lệ. | 0% email/SMS/Zalo request được phát đi. Các thao tác hoàn toàn gói gọn trong container local UAT. | **`PASS`** | Bảo đảm không làm phiền hay gây hoang mang cho người dân ngoài đời thực. |
| **`TC-UAT-14`** | **Toàn Bộ 8 Cases** | **Kiểm Chứng An Toàn Hiển Thị Lỗi Backend Trên Frontend (`Clear Error Handling`)** | 1. Cố tình gửi request sai lệch hoặc bấm hoàn thành khi hồ sơ chưa đủ điều kiện thông qua DevTools.<br>2. Quan sát phản hồi trên UI. | Thông báo lỗi backend được bắt (`caught`) và hiển thị thành banner/toast dễ hiểu trên giao diện, không bị crash ứng dụng (`White Screen`). | Giao diện hiển thị Toast cảnh báo màu đỏ rõ ràng với thông điệp từ API (`Hồ sơ chưa đáp ứng đủ điều kiện hoàn thành nghĩa vụ tài chính`). | **`PASS`** | Trải nghiệm người dùng (`UX`) an toàn, minh bạch khi xử lý lỗi. |

---

## 2. Rà Soát Đối Chiếu 16 Tiêu Chí An Toàn Bắt Buộc (`16 Mandatory Safety Criteria Verification`)
- [x] **1. Tab/section "Nghĩa vụ tài chính" hiển thị:** `PASS` (Hiển thị mượt mà trên 8/8 demo cases).
- [x] **2. Safety banner hiển thị:** `PASS` (Banner vàng `DEMO ONLY - NOT REAL CITIZEN DATA` hiển thị rõ ràng).
- [x] **3. Có nhãn: `DỰ KIẾN - CHƯA PHẢI SỐ TIỀN CHÍNH THỨC`:** `PASS` (Ghi nhận trên toàn bộ các khoản mục AI draft).
- [x] **4. Demo estimate không bị hiểu là số tiền chính thức:** `PASS` (`isEstimate = true`, `officialTotalAmount = null`).
- [x] **5. Draft không tạo `officialAmount`:** `PASS` (`officialAmount = null` trên tất cả draft items).
- [x] **6. Draft không tạo `officialTotalAmount`:** `PASS` (`officialTotalAmount = null` trên assessment tổng).
- [x] **7. Không có nút "Phát hành thông báo thuế":** `PASS` (Hệ thống không can thiệp thẩm quyền phát hành của Chi cục Thuế).
- [x] **8. Không có nút "AI xác nhận số tiền chính thức":** `PASS` (AI chỉ đóng vai trò gợi ý tham khảo ban đầu).
- [x] **9. Không có nút "Tự gửi thông báo cho công dân":** `PASS` (Hệ thống không tự ý gửi SMS/Email ra bên ngoài).
- [x] **10. Không completed khi thiếu thông báo thuế demo nếu case cần thông báo thuế:** `PASS` (Chốt chặn khóa 100% trên case `DEMO-FO-UAT-01`, `02`).
- [x] **11. Không completed khi thiếu chứng từ demo:** `PASS` (Chốt chặn khóa 100% trên case `DEMO-FO-UAT-03`).
- [x] **12. Không completed khi chưa officer verified:** `PASS` (Chốt chặn khóa 100% trên case `DEMO-FO-UAT-04`).
- [x] **13. Completed chỉ thành công với case đủ điều kiện:** `PASS` (Chỉ mở khóa cho case `DEMO-FO-UAT-05` và `06`).
- [x] **14. Audit log ghi nhận hành động quan trọng:** `PASS` (Nhật ký ghi vết đầy đủ thao tác khởi tạo và xác nhận).
- [x] **15. Error backend hiển thị rõ trên frontend:** `PASS` (Thông báo lỗi trả về rõ ràng, không crash giao diện).
- [x] **16. Không có wording gây hiểu nhầm là số tiền chính thức:** `PASS` (Toàn bộ văn bản đều kèm từ khóa `Mô phỏng / Giả lập / Dự kiến`).
