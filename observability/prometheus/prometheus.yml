global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'legalflow_backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:3000']
    authorization:
      credentials_file: /etc/prometheus/token.txt
